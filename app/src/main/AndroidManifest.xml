<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.example.uidproject">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.UIDProject">
        <activity
            android:name=".addCar.AddCar"
            android:exported="false" />
        <activity
            android:name=".login.LoginPage"
            android:exported="true"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".wallet.WalletPage"
            android:exported="false" />
        <activity
            android:name=".profilePage.ProfilePage"
            android:exported="false" />

        <activity
            android:name=".create_route.RouteSharedActivity"
            android:exported="false"
            android:label="@string/title_activity_route_started"
            android:theme="@style/Theme.UIDProject.NoActionBar" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="Home Activity">
        </activity>

        <activity
            android:name=".principal_screen.PrincipalScreenActivity"
            android:exported="true"
            android:label="Home Activity">
        </activity>
        <activity
            android:name=".ProfileActivity"
            android:exported="true"
            android:label="Profile">
        </activity>

        <activity
            android:name=".join_route.screens.ChooseRoute"
            android:exported="true"
            android:label="Choose Route">
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="Settings">
        </activity>

        <activity
            android:name=".create_route.CreateRouteActivity"
            android:exported="true"
            android:label="Create Route">
        </activity>

        <activity
            android:name=".create_route.PickTransportationActivity"
            android:exported="true"
            android:label="Pick Transportation">
        </activity>

        <activity
            android:name=".create_route.CreateRouteCarActivity"
            android:exported="true"
            android:label="Create Car">
        </activity>

        <activity
            android:name=".create_route.CreateRouteBusActivity"
            android:exported="true"
            android:label="Create bus transit">
        </activity>

        <activity
            android:name=".join_route.screens.RouteSummary"
            android:exported="false" />
        <activity
            android:name=".join_route.screens.OthersProfile"
            android:exported="false" />
        <activity
            android:name=".join_route.screens.TheirBuddies"
            android:exported="false" />
        <activity
            android:name=".join_route.screens.Reviews"
            android:exported="false" />
        <activity
            android:name=".join_route.screens.JoinedRoute"
            android:exported="false" />

        <activity android:name=".friend_list.FriendListActivity"
            android:exported="false">
        </activity>

        <activity android:name=".chat.ChatActivity"
            android:exported="false">
        </activity>
        <activity android:name=".keep_buddy.KeepBuddyActivity"
            android:exported="false">

        </activity>
    </application>

</manifest>